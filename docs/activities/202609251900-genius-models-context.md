# Genius models and context — 2026-09-25

Issue [#64](https://github.com/sufficit/sufficit-vscode-symposium/issues/64) fills the Genius model picker from the native `genius models --json` command. Catalog calls receive the current Sufficit Identity bearer through the child process environment, as chat turns already do. The picker displays preset titles and retains `default` for server-managed selection. Streamed usage gets a context window only when the catalog reports one for the effective or explicitly selected preset. Unknown windows stay unavailable rather than showing a fabricated percentage. Version is `2026.925.6`.

The new catalog test failed before implementation and passed afterward. `COVERAGE_BASE_SHA=$(git merge-base origin/develop HEAD) npm run verify:package` passed, including lint, typecheck, tests, changed-line coverage, and VSIX checks. The resulting VSIX is installed on the development code-server alongside Genius CLI `0.127.0`. Its Genius resident service remains active with two existing sessions. An already open code-server window needs a reload to activate the installed extension. An authenticated end-to-end catalog request from the live extension still requires a user session; the automated test verifies delegated auth with a fake CLI.
